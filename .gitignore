# python / desktop
__pycache__/
*.pyc
build/
dist/
*.spec
*.zip
.claude/

# backend (FastAPI)
backend/.venv/
backend/.env
backend/*.db
backend/*.db-journal

# frontend (SvelteKit) — node_modules etc. are handled by frontend/.gitignore
frontend/.env

